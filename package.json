{
  "name": "dsh-agentic-report-generator",
  "version": "1.0.0",
  "description": "DeepSeek Harness Native Runtime for Financial Reports",
  "private": true,
  "scripts": {
    "build": "tsc -b",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@nanmicoder/dsh-agent-teams": "^0.1.15",
    "cordis": "^3.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0"
  }
}